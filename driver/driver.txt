package elev_controller


/*
#include "elev.h"
#include "io.h"
*/
import "C"
import(
	."../definitions"
)

func Elev_controller(queue_message chan Elevator,status_message chan Elevator){
	var current_queue [FLOORS]int
	button_press = make(chan Order)


	go check_for_input(button_press)
	go go_to_floor(queue [FLOORS]int)



	select{
		case elev:= <- queue_message:
			current_queue = elev.queue
			go go_to_floor(current_queue)
		case input:= <- button_press:
			message = Elevator{}
			status_message <- input

	}
}

func go_to_floor(queue [FLOORS]int) {

	
}

func check_for_input(input chan Order){

}

func set_lights(Elevator queue_message){

}
